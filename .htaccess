<IfModule mod_rewrite.c>
    RewriteEngine On

    # Prevent infinite loop by checking if the request is already inside the public folder
    RewriteCond %{REQUEST_URI} !/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
